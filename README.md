# mathjs_examples
Examples for math.js(tested on Math Mode plugin for Joplin mobile - Android). Math.js code is evaluated in a Math.js parser and that fact causes some limitations on what can be parsed/evaluated successfully, as opposed to evaluation in a browser environment.
